# dependencies
node_modules/
/.pnp
.pnp.js

# testing
coverage/

# next.js
.next/
out/
build/

# production
dist/

# env files (NEVER commit secrets)
.env
.env*.local
.env.production

# Catch-all for stray secret files. Keep ANY file that mentions a credential
# in its name out of git, even if someone forgets to put it in .env.local.
*API_KEY*
*SECRET*
*.key
*.pem
*credentials*

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# IDE
.idea/
*.swp
*.swo
.DS_Store

# logs
*.log
npm-debug.log*

# local infra data
infra/data/

# local uploads (PDFs)
uploads/

# Sample lab invoices contain real patient names. Never commit. The path is
# referenced by the extractor's tests and is provisioned out-of-band.
samples/pdfs/
